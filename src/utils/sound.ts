// Sons de création — expo-audio non disponible en SDK 54 (disponible à partir de SDK 55).
// Ce stub est un no-op propre : aucun crash, aucun import manquant.
// À brancher quand le projet monte en SDK 55+.
export async function playCreationSound(_metal: 'gold' | 'silver'): Promise<void> {
    // no-op — expo-audio incompatible SDK 54
}
